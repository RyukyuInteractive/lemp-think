# ThinkPHP in LEMP

run:

```
$ git clone git@github.com:RyukyuInteractive/lemp-think.git
$ cd lemp-think/code
$ composer install
$ docker-compose up
```

open [localshot:8080](http://localshot:8080)
